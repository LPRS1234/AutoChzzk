# AutoChzzk 작업 규칙

## 사용자 작업 보호

- 기존 파일과 사용자가 작업 중인 변경사항을 임의로 되돌리거나 덮어쓰지 않는다.
- 요청받지 않은 코드 정리, 대규모 포맷 변경, 리팩터링 또는 관련 없는 수정을 하지 않는다.
- 삭제, 강제 덮어쓰기, 초기화, 강제 푸시처럼 복구하기 어려운 작업은 실행 직전에 다시 확인한다.
- 비밀번호, API 키, 토큰 및 개인정보를 출력하거나 코드와 저장소에 기록하지 않는다.

## 작업 방식

- 작업 위치는 이 저장소 루트(`AutoChzzk`)를 기준으로 한다.
- 사용자가 “원인 분석만”을 요청하면 코드나 설정을 수정하지 않고 원인과 확인 결과만 보고한다.
- 변경 후 가능한 범위에서 관련 테스트, 빌드 또는 검사를 실행한다. 실패한 검증은 원인과 함께 숨기지 않고 보고한다.
- 완료 보고에는 변경 내용, 검증 결과, 수행하지 못한 항목을 간결하게 포함한다.

## Git 및 GitHub

- 커밋 메시지는 한국어로 작성한다.
- 변경 후 `origin` 원격을 확인하고, 현재 작업과 직접 관련된 파일만 검토해 커밋·푸시한다.
- 강제 푸시나 히스토리 재작성을 하지 않는다.
- 배포와 GitHub Release 생성은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 버전 및 배포

- 시맨틱 버저닝을 따른다.
  - 버그 수정 또는 작은 UI 수정: 패치 버전 증가 (예: `1.2.3` → `1.2.4`)
  - 하위 호환되는 기능 추가: 마이너 버전 증가 (예: `1.2.3` → `1.3.0`)
  - 호환성을 깨는 변경: 메이저 버전 증가 (예: `1.2.3` → `2.0.0`)
- 배포 시 앱 버전, Inno Setup 버전, README의 현재 버전을 같은 값으로 맞춘다.
- 다음 배포부터 Windows 실행 파일 속성에 다음 항목을 포함한다.
  - 제품 이름: `AutoChzzk`
  - 파일 설명: `저장한 채널의 방송 시작을 자동 감지합니다`
  - 파일 버전 및 제품 버전: 배포 버전과 동일한 값
- Windows 실행 파일 속성에 저작권 항목은 넣지 않는다.

## 프로젝트 개요

- AutoChzzk는 저장한 치지직 채널의 방송 시작과 종료를 감지하고, 선택한 Chrome 프로필의 확장 프로그램과 연동해 방송 탭을 자동으로 열고 닫는 Windows용 Python 데스크톱 앱이다.
- 데스크톱 앱은 Tkinter UI, 백그라운드 방송 감지, Chrome 프로필 선택, 로컬 확장 프로그램 서버, 자동 업데이트 기능으로 구성된다.

## 주요 파일과 디렉터리의 역할

- `autochzzk.py`: 앱 진입점이자 `AutoChzzkApp` UI, 상태 관리, 방송 감지 주기, Chrome 및 확장 프로그램 연동을 조율한다.
- `autochzzk_core/config.py`: 앱 이름·버전·경로·URL·정규식 같은 공통 상수와 Windows DPI 설정을 관리한다.
- `autochzzk_core/chzzk_api.py`: 채널 ID 추출, 치지직 채널/방송 조회, 최신 릴리스 조회를 담당한다.
- `autochzzk_core/storage.py`: 채널 목록과 앱 설정을 JSON으로 불러오고 저장한다.
- `autochzzk_core/chrome_profiles.py`: 로컬 Chrome 프로필 정보를 찾아 앱에서 사용할 목록으로 만든다.
- `autochzzk_core/extension.py`: 앱과 Chrome 확장 프로그램 사이의 `127.0.0.1` 로컬 통신, 탭 상태 추적, 탭 열기·닫기 요청을 처리한다.
- `autochzzk_core/updater.py`: 업데이트 메타데이터 해석, 설치 파일 다운로드, 크기 및 SHA-256 검증, 설치 프로그램 실행을 담당한다.
- `autochzzk_core/widgets.py`와 `autochzzk_core/changelog.py`: 재사용 UI 위젯과 버전별 변경 내역을 제공한다.
- `chrome_extension/`: 시청 중인 치지직 탭을 앱에 알리고 앱의 탭 제어 요청을 수행하는 Chrome 확장 프로그램이다.
- `tests/`는 핵심 동작의 자동 검사를, `installer/`는 Windows 설치 파일과 실행 파일 메타데이터 구성을 담당한다.

## 코드 분석 및 설명 방식

- 한 파일만 고립해서 설명하지 말고, 필요하면 진입점부터 관련 호출자와 피호출자를 따라가며 실제 실행 순서를 설명한다.
- UI 메인 스레드, 백그라운드 작업, 로컬 HTTP 요청처럼 실행 주체가 나뉘는 부분은 각 작업이 언제 시작되고 결과가 UI에 어떻게 반영되는지 구분한다.
- `if`는 조건에 따라 어느 경로가 선택되는지, `for`는 무엇을 어떤 순서로 반복하는지, `continue`는 어떤 경우 현재 반복을 건너뛰는지 설명한다.
- `return`은 함수가 끝나는 시점과 돌려주는 값을, `try`/`except`는 정상 경로와 예외 발생 시 대체 경로를 나누어 설명한다.
- 중요한 변수는 값의 자료형만 말하지 말고 무엇을 나타내는지, 어디에서 만들어지고 바뀌며 얼마나 유지되는지 설명한다.
- 주요 함수와 메서드는 목적, 호출 시점, 인자, 반환값, 파일 저장·네트워크 요청·UI 변경 같은 부수 효과를 함께 설명한다.
- 코드에서 확인한 사실과 문맥을 바탕으로 한 추론을 구분하고, 확실하지 않은 동작은 단정하지 않는다.

## 수정 규칙

- 함수 시그니처, 저장 데이터 구조, 확장 프로그램 요청·응답 형식을 바꾸기 전에는 모든 호출 위치와 관련 테스트를 확인한다.
- Tkinter 위젯 갱신은 메인 스레드에서 이뤄지는 기존 구조를 유지하고, 백그라운드 작업에서 UI를 직접 조작하지 않는다.
- 앱과 Chrome 확장 프로그램 중 한쪽의 통신 형식이나 버전 판정을 바꾸면 다른 쪽과의 호환성도 함께 점검한다.
- 사용자에게 보이는 문구를 바꿀 때는 상태 표시, 오류 안내, 설치 안내가 실제 동작과 일치하는지 확인한다.

## 데이터 안전 규칙

- `channels.json`과 `settings.json`의 기존 필드 및 알 수 없는 추가 필드를 가능한 한 보존하며, 데이터 마이그레이션이 필요하면 이전 형식을 읽을 수 있는 경로를 마련한다.
- 개발용 실행의 프로젝트 폴더와 배포용 EXE의 `%LOCALAPPDATA%\AutoChzzk`는 저장 위치가 다르므로, 데이터를 조사하거나 수정하기 전에 실제 사용 중인 경로를 먼저 확인한다.
- JSON 파싱 실패나 일부 값의 누락을 빈 데이터로 확정해 기존 파일에 즉시 저장하지 말고, 원본을 보존한 상태에서 복구 가능성을 먼저 확인한다.
- 채널 목록, Chrome 프로필 식별 정보, 로컬 확장 프로그램 요청 내용은 디버그 출력이나 테스트 샘플에 실제 사용자 값 그대로 남기지 않는다.

## Windows 호환성

- 경로 처리는 `pathlib`과 Windows 환경 변수를 우선 사용하고, 사용자 이름이나 특정 PC의 절대 경로를 코드에 고정하지 않는다.
- 백그라운드 프로세스나 명령을 추가할 때는 배포용 GUI 앱에서 불필요한 콘솔 창이 나타나지 않는지 확인한다.
- 파일 위치나 실행 방식을 바꾸면 Python 소스 실행뿐 아니라 패키징된 EXE, Inno Setup 설치 경로, 동봉된 `chrome_extension` 경로에서도 동작하는지 점검한다.

## 학습 목적의 설명 선호

- 사용자는 Python을 배우는 고등학생이므로, 처음 등장하는 용어는 쉬운 한국어로 풀고 필요한 전문 용어는 뜻을 함께 적는다.
- 전체 흐름을 먼저 짧게 보여 준 뒤 세부 코드로 내려가며, 문법 설명은 가능하면 이 프로젝트의 실제 값이나 짧은 예시를 사용한다.
- 단순히 “무엇을 한다”로 끝내지 말고 왜 그 조건, 반복, 예외 처리, 반환 방식이 필요한지 연결해서 설명한다.
- 한 번에 지나치게 많은 개념을 전제하지 말고, 이해에 필요한 선행 개념이 있으면 해당 개념부터 단계적으로 설명한다.
