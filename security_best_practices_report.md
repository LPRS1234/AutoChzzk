# AutoChzzk 유지보수·보안 검토

## 수정 진행 결과 — 2.0.0

아래 최초 검토 내용과 줄 번호는 수정 전 코드에 대한 기록이다. 후속 요청에 따라 코드와 회귀 테스트를 수정했으며, 적용 내용은 다음과 같다.

| 항목 | 적용 내용 |
|---|---|
| 1·8 저장 데이터 | 읽기 오류를 `StorageError`로 구분, 손상 시 저장 차단, 시작 시 무조건 저장 제거, 원본 재검증·백업·원자 교체, 알려진 필드 타입 검사와 추가 필드 보존 |
| 2 API 상태 | 성공 코드·객체·OPEN/CLOSE 상태 검사, 잘못된 응답은 조회 실패 처리, 이전 방송 상태 유지 |
| 3 명령 누적 | 서버 확인 후 완료 ID 제거, 크기 제한 및 세션 중복 실행 기록, 재시작·장시간 사용 회귀 테스트 |
| 4 로컬 인증 | 사용자 연결 코드, Windows 사용자별 DPAPI 보관, 요청/응답 HMAC 검증·재전송 차단, 개인정보 없는 서버 확인 후 상태 보고, 방송 URL 허용 목록 |
| 5 HTTP 처리 | Host/Origin·타입·크기 검사, 초과 요청 거부, 소켓 타임아웃, 동시 요청 수 제한 |
| 6 UI 응답성 | 채널 이름 조회를 백그라운드로 이동하고 저장·화면 반영은 메인 스레드에서 처리 |
| 7 감지 경쟁 | 최대 4개 동시 조회, 채널별 중복 방지, 메인 스레드에서 결과 반영, 변경·삭제된 채널의 오래된 결과 무시 |
| 프로필 | 표시 이름 충돌 방지, 계정 식별값 변경 시 재연결 확인, 읽기 오류·일시 파일 부재 시 선택 보존 |
| 요청·명령 수명 | 확장 요청 중단 시간 제한, 미완료 열기·닫기 명령 만료 및 큐 크기 제한 |
| 빌드 | 런타임·빌드 의존성 버전 고정, Python 기준 버전과 검증 명령 문서화, EXE 런타임 로고 리소스 포함 |
| 테스트·구조 | UI 없이 검사 가능한 조회 관리 모듈 분리, 저장/API/HTTP/확장/감지/프로필 회귀 테스트 추가 |

인증을 지원하지 않는 이전 확장 프로그램과 호환되지 않으므로 앱·확장은 **2.0.0**으로 맞췄다. 기존 채널·설정 형식은 유지되지만, 확장 업데이트 후 앱의 **확장 연결 코드**를 각 Chrome 프로필의 확장 설정에 한 번 등록해야 한다.

연결 코드는 저장소 밖의 `%LOCALAPPDATA%/AutoChzzk/extension_pairing.key`에 Windows DPAPI로 보호하여 저장한다. 확장 쪽 저장 접근도 확장의 신뢰된 실행 환경으로 제한한다. 이미 같은 Windows 사용자 권한으로 계정 전체를 제어하는 악성 프로그램까지 격리하는 보안 경계는 아니다.

### 검증 범위

- 실제 사용자 채널·설정·Chrome 프로필·연결 코드를 읽지 않고 모의 객체와 임시 파일을 사용했다.
- Python 회귀 테스트 **57개**, Node 회귀 테스트 **8개**가 모두 통과했다. 명령 누적 테스트는 **2,560개** 명령을 처리했다.
- 숨긴 Tk 창의 초기화·연결 안내 구성, 고정된 별도 가상환경의 Windows EXE 빌드와 리소스·버전 속성 확인을 수행했다. 최종 소스로 **Inno Setup 설치 파일 빌드도 성공**했다. 기존 `dist`·`release`를 덮어쓰지 않고 임시 디렉터리에 검증 산출물을 만들었다.
- 실제 Chrome 프로필에서의 최초 연결·탭 자동 제어와 설치 프로그램의 실제 설치·업데이트 실행은 수행하지 않았다. 이 항목은 별도의 수동 확인이 필요하다.
- 기존 사용자 수정은 보존했다. 이 보고서의 검증 단계에서는 커밋·푸시·배포를 다루지 않으며, 배포 결과는 GitHub Release 기록을 기준으로 확인한다.

---

## 최초 검토 기록

검토일: 2026-09-10. 대상: 현재 작업 폴더의 소스 코드. 기존 수정 상태인 `autochzzk_core/chzzk_api.py`, `autochzzk_core/storage.py`도 현재 내용으로 검토했으며 수정하지 않았다. 이 문서만 추가했다.

## 요약

가장 먼저 개선할 부분은 손상된 채널 파일의 자동 덮어쓰기, 잘못된 API 응답을 방송 종료로 판단하는 처리, 확장 프로그램의 완료 명령 누적이다. 보안에서는 로컬 통신 상대를 인증하지 않는 구조와 HTTP 입력·자원 제한이 주요 문제다. 인터넷에서 즉시 원격 코드 실행이 가능하다는 증거는 확인하지 않았다.

심각도는 이 프로젝트에서의 영향과 발생 조건을 기준으로 한 검토 판단이다. ‘높음’인 데이터·운영 문제와 ‘중간’인 조건부 로컬 보안 문제를 구분했다.

## 구조와 실행 흐름

| 구성 | 역할 |
|---|---|
| `autochzzk.py` | 1,010줄의 진입점·UI·감지·업데이트·Chrome 실행 조율 코드 |
| `autochzzk_core/config.py` | 버전, API 주소, 개발/EXE 데이터 경로, 리소스 경로 |
| `storage.py`, `chzzk_api.py`, `chrome_profiles.py` | 저장 데이터, 외부 API, Chrome 프로필 조회 |
| `extension.py` | 로컬 HTTP 서버, 연결 상태와 탭 명령 큐 |
| `updater.py` | 업데이트 메타데이터 검사, 다운로드·해시 검사, 설치 실행 |
| `widgets.py`, `changelog.py` | 재사용 UI와 변경 내역 |
| `chrome_extension/` | 서비스 워커의 상태 보고·탭 제어, 주기적 연결, 페이지 자동 재생 |
| `tests/` | 업데이트 9개, 확장 상태 클래스 4개 테스트 |
| `AutoChzzk.spec`, `installer/` | PyInstaller 패키징 및 Inno Setup 설치 설정 |

앱 시작 시 설정·프로필·채널을 읽고 로컬 서버와 UI를 준비한다. 초기 조회와 정기 감지는 별도 백그라운드 스레드에서 실행한다. API 결과는 주로 `_ui()` → `root.after()`를 통해 UI 메인 스레드에 전달한다. 방송 시작/종료는 명령 큐에 반영되고, 확장 프로그램이 로컬 서버에 상태를 보고할 때 명령을 받아 실제 탭을 조작한다. 업데이트 조회·다운로드도 별도 스레드에서 처리한다.

Python Tkinter·표준 HTTP 서버 및 Chrome 확장 프로그램 조합에 정확히 대응하는 보안 스킬 참고 문서는 없었다. 일반 JavaScript 보안 지침은 적용 가능한 범위만 사용하고, 구체적인 판단은 코드와 공식 플랫폼 문서에 근거했다.

## 높은 우선순위: 데이터·운영 안정성

### 1. 손상된 채널 파일을 앱 시작만으로 빈 목록으로 덮어쓸 수 있음

- 근거: `autochzzk_core/storage.py:9–27`, `autochzzk.py:82–84`.
- `load_channels()`는 JSON 해석 실패와 파일 부재를 모두 `[]`로 반환한다. 생성자는 그 결과를 곧바로 `_save_channels()`로 저장한다.
- 영향: 복구 가능한 손상 파일도 앱 실행 시 `[]`로 덮어써 원본을 잃을 수 있다. 잘못된 항목을 건너뛴 결과도 자동 저장되므로 일부 항목만 조용히 사라질 수 있다.
- 채널·설정 저장은 원본에 직접 `write_text()`한다. 저장 중 종료·디스크 오류가 나면 불완전한 파일이 남을 수 있다. 설정도 읽기 실패 후 나중에 저장하면 원래 필드를 잃을 수 있다.
- 재현: 실제 파일 대신 모의 경로에서 `'['`를 읽게 했을 때 `[]` 반환을 확인했다. 앱 생성자의 즉시 저장 경로는 코드로 확인했으며 실제 사용자 파일은 덮어쓰지 않았다.
- 개선 방향: 파일 없음/손상/정상 결과를 구분하고, 손상 시 저장을 막는다. 원본 백업 및 같은 디렉터리의 임시 파일을 완성한 뒤 교체하는 저장 방식을 사용한다.

### 2. 잘못된 API 응답을 방송 종료로 판단하거나 감지 스레드가 중단됨

- 근거: `autochzzk_core/chzzk_api.py:27–42`, `autochzzk.py:849–888`.
- 응답에서 `content`가 없거나 비어 있으면 `{}`로 바꾼다. `status == "OPEN"`이 아니면 모두 방송 종료로 취급한다.
- 영향: 방송 중인 상태에서 HTTP 요청 자체는 성공했지만 응답 내용이 잘못되면 종료로 잘못 판단해 앱이 연 탭을 닫을 수 있다. `content`가 비어 있지 않은 배열이면 `AttributeError`가 나며, 감지 루프의 예외 처리에 포함되지 않아 해당 스레드가 끝날 수 있다.
- 재현: 가짜 응답 `{}` 및 `{"content":[]}`는 오프라인으로 반환했고, `{"content":[1]}`은 `AttributeError`가 발생했다. 실제 치지직 API가 이런 응답을 현재 보내고 있다는 뜻은 아니다.
- 개선 방향: 최상위 객체, `content`, 상태값을 검사한다. 조회 실패는 ‘상태 불명’으로 구분하여 이전 정상 상태를 유지하고, 감지 작업이 멈추면 UI에 알린다.

### 3. 완료 명령 누적으로 확장 연결이 장시간 실행 중 실패할 수 있음

- 근거: `chrome_extension/background.js:3`, `79–98`, `112–117`; `autochzzk_core/extension.py:179–180`.
- `completedCommandIds`는 추가만 하고 지우지 않는다. 매 요청에 모든 ID를 포함하지만 서버는 본문을 최대 16,384바이트까지만 읽는다.
- 영향: 서비스 워커가 살아 있는 동안 명령이 누적되면 잘린 JSON을 해석하다 실패한다. 이후 정상 상태 보고와 명령 처리가 중단될 수 있다. 워커가 재시작하면 메모리가 초기화되므로 항상 일정 시간 뒤 발생한다고 단정할 수는 없다.
- 확인: 합성한 32자리 명령 ID 500개의 요청은 Python JSON 직렬화 기준 18,057바이트로 제한을 초과했다. JavaScript의 공백 없는 직렬화에서도 500개 ID만으로 제한을 넘는다. 실제 Chrome 장시간 실행 시험은 하지 않았다.
- 개선 방향: 서버가 수신 확인한 완료 ID는 제거하고, 중복 실행 방지용 기록은 별도의 크기·시간 제한으로 관리한다. 초과 요청은 자르지 말고 명시적으로 거부한다.

## 중간 우선순위: 보안

### 4. 로컬 서버와 확장 프로그램이 서로의 신원을 확인하지 않음

- 근거: `autochzzk_core/extension.py:154–208`, `217–225`; `chrome_extension/background.js:2`, `79–117`.
- 서버는 인증 없이 상태 보고를 받고, 클라이언트가 제출한 이메일/계정 ID를 프로필 식별에 사용한다. Origin 검사가 없고 CORS 응답은 `*`다. 확장도 고정 포트의 응답을 신뢰하며, 명령 URL을 치지직 방송 URL로 제한하지 않는다.
- 조건과 영향: 선택된 프로필 식별값을 아는 로컬 프로그램은 가짜 상태 보고로 시청 여부나 명령 전달을 교란할 수 있다. 앱이 꺼진 사이 다른 로컬 프로그램이 해당 포트를 선점하면 확장의 계정 식별값·채널 보고를 받거나 임의 HTTPS 탭 열기 명령을 줄 수 있다. 이는 조건부 시나리오이며 실제 공격은 실행하지 않았다.
- 외부 웹사이트에서의 접근은 브라우저의 로컬 네트워크 접근 정책에도 영향을 받으므로, CORS `*`만으로 모든 웹사이트가 바로 공격 가능하다고 단정하지 않는다. `127.0.0.1` 바인딩은 외부 PC의 직접 접속을 제한하는 긍정적 조치다.
- 개선 방향: 신뢰할 수 있는 초기 페어링과 상호 인증, Origin/Host 검사, 명령 스키마 및 URL 허용 목록을 도입한다. 이메일은 인증 비밀값으로 취급하면 안 된다.
- 참고: Chrome의 호스트 권한이 탭 생성 URL의 허용 목록을 대신하지는 않는다. [Chrome Tabs API](https://developer.chrome.com/docs/extensions/reference/api/tabs).

### 5. HTTP 요청 형식 검사와 연결 자원 제한이 부족함

- 근거: `autochzzk_core/extension.py:166–210`, `217–225`.
- JSON 최상위가 객체인지 확인하지 않고 `.get()`을 호출한다. 배열 필드가 실제 배열인지도 확인하지 않는다. 요청 본문 읽기에 명시적인 연결 타임아웃이 없으며 요청별 스레드의 동시 실행 제한도 없다.
- 재현: 메모리상의 요청 처리기에 `[]`를 전달하면 `AttributeError`, `channelIds: null`을 전달하면 `TypeError`가 발생했다. 두 예외 모두 현재 처리 대상이 아니다. 이것만으로 서버 전체가 종료된다고 해석해서는 안 된다.
- 영향: 잘못된 요청이 정상적인 400 응답 대신 예외로 끝난다. 로컬 클라이언트가 본문을 보내지 않은 채 연결을 유지하는 경우 자원이 점유될 수 있다. 실제 서버에 부하를 주는 시험은 수행하지 않았다.
- 개선 방향: 최상위·각 필드의 타입과 크기 검사, 초과 길이의 명시적 거부, 본문 읽기 타임아웃과 동시 요청 제한을 추가한다.
- 참고: Python 문서도 `http.server`가 기본적인 보안 검사만 제공한다고 설명한다. 라이브러리 사용 자체보다 현재 처리기에 빠진 제어가 문제다. [Python HTTP 서버 문서](https://docs.python.org/3/library/http.server.html).

## 중간 우선순위: 유지보수

### 6. 채널 추가 중 UI가 네트워크 응답을 기다리며 멈춤

- 근거: `autochzzk.py:739–752`; `autochzzk_core/chzzk_api.py:27–37`.
- 버튼에서 실행되는 `add_channel()`이 메인 스레드에서 `get_channel_name()`을 호출한다. `update_idletasks()`는 이 네트워크 호출을 비동기로 바꾸지 않는다.
- 영향: 응답 지연 중 창 이동·버튼·예약된 UI 작업의 처리가 지연된다. 네트워크 타임아웃은 15초지만 전체 작업 시간이 반드시 15초 이내라는 뜻은 아니다.
- 개선 방향: 기존 다른 조회 작업처럼 백그라운드에서 이름을 조회하고, 결과만 메인 스레드로 돌려준다.

### 7. 여러 감지 작업이 상태를 공유하고 채널 조회가 직렬로 진행됨

- 근거: `autochzzk.py:89–118`, `849–888`.
- 초기 조회와 정기 조회가 별도 스레드이며 같은 `was_live`를 읽고 쓴다. 초기 조회 완료를 기다리는 신호 없이 `last_checked` 시각으로만 정기 조회를 늦춘다.
- 영향: 채널 수가 많거나 API가 느리면 초기 조회가 끝나기 전에 정기 조회가 같은 채널을 확인할 수 있다. 응답 순서가 뒤바뀌면 오래된 상태가 최신 상태를 덮는 경쟁 상태가 가능하다. 실제 중복 탭 발생까지 재현한 것은 아니다.
- 정기 루프는 채널을 하나씩 조회하므로 앞 채널의 지연이 뒤 채널의 감지를 늦춘다. 설정한 간격은 정확한 실행 주기를 보장하지 못한다.
- 개선 방향: 채널별 진행 중 조회를 관리하고 결과 순서를 검증한다. 상태 변경을 한 실행 주체에 모으고, 필요하면 동시 조회 수에 제한을 둔다.

### 8. 저장 데이터의 타입 검증 누락으로 앱 시작이 실패할 수 있음

- 근거: `autochzzk_core/storage.py:9–23`; `autochzzk.py:82`.
- 최상위 값이 목록인지, `id`가 문자열인지 확인하지 않는다. JSON 문법상 유효한 데이터라도 `null` 또는 숫자 ID를 만나면 처리되지 않는 예외가 난다.
- 재현: 합성 데이터 `null`과 `[{"id":123}]`에서 모두 `TypeError`가 발생했다.
- 개선 방향: 필드별 검증 규칙과 복구 정책을 정하되, 잘못된 항목을 발견했다고 원본에서 자동 삭제하지 않는다. 항목 오류와 파일 전체 오류도 구분한다.

## 낮은 우선순위 및 추가 점검 후보

- **테스트 범위:** 현재 13개는 업데이트와 `ChromeTabState` 중심이다. 저장 복구, HTTP 요청 처리기, 감지 전이, JavaScript 명령 수명, UI와 스레드 연결은 검증하지 않는다. 이번에 재현한 실패 조건부터 회귀 테스트 대상으로 삼는 편이 효과적이다.
- **진입점 책임 집중:** `autochzzk.py` 한 클래스가 UI, 감지, 업데이트 안내, 프로필·프로세스 관리까지 담당한다. 단순 파일 길이보다 UI 없이 핵심 동작을 시험하기 어려운 결합이 문제다. 추후 관련 기능 수정 시 감지 상태와 UI를 점진적으로 분리할 수 있다.
- **프로필 갱신:** `autochzzk.py:279–292`는 디렉터리와 표시 이름만 비교한다. 계정 식별값만 바뀌면 연결 키가 갱신되지 않을 수 있다. `chrome_profiles.py:23–24`의 표시 이름이 같으면 `profile_labels` 사전에서 항목이 겹칠 수도 있다.
- **미완료 닫기 명령:** `extension.py:22`, `98–114`, `130–145`의 닫기 큐는 확인 응답 외의 만료 처리가 없다. 클라이언트가 사라지면 명령이 남고, 다시 연결되면 오래된 종료 명령이 전달될 수 있다.
- **확장 요청 타임아웃:** `background.js:103–121`의 `fetch`가 끝나지 않으면 `reporting`이 계속 참이어서 후속 보고를 건너뛴다. 요청 중단 시간 제한이 필요하다.
- **빌드 재현성:** `requirements.txt:1–2`는 하한 버전만 지정한다. 같은 소스도 빌드 시점에 다른 의존성을 받을 수 있다. 릴리스에 사용한 Python·PyInstaller·라이브러리 버전을 기록하고 고정하는 방안을 권한다. 설치된 패키지의 특정 취약점 여부는 이번 검토에서 확인하지 않았다.
- **리소스 패키징:** `AutoChzzk.spec:8`의 `datas=[]`와 `config.py`의 PNG/ICO 리소스 경로가 연결되어 있지 않다. EXE 아이콘 지정은 런타임 이미지 파일 포함과 별개다. 이 spec으로 빌드할 때 UI 이미지 포함 여부를 확인해야 한다. 기존 배포 파일을 직접 검사하지 않아 배포본 누락으로 단정하지 않는다.

## 잘 되어 있는 부분

- 로컬 서버가 모든 네트워크 인터페이스가 아닌 `127.0.0.1`에 바인딩되어 있다.
- 확장이 종료하는 탭은 세션에 기록된 앱 자동 열기 탭으로 제한한다.
- 업데이트는 HTTPS GitHub 주소, 파일 이름·크기·SHA-256을 검사하고, 설치 직전에도 해시를 재검사한다. 부분 다운로드 파일을 완성 후 교체하는 구조도 있다.
- 데이터 경로는 개발 실행과 EXE 실행을 구분한다. 정상 채널 항목의 알 수 없는 추가 필드는 로더가 그대로 보존한다.
- UI 갱신은 대체로 `root.after()`로 전달하며, 네트워크 작업의 상당 부분은 백그라운드로 분리되어 있다.

## 검증 및 한계

- `python -B -m unittest discover -s tests -v`: 13개 모두 통과.
- 실제 데이터 파일 대신 모의 객체와 메모리 데이터로 저장 로더·API·HTTP 입력 오류를 재현했다.
- 기존 테스트의 다운로드·설치 실행은 모의 처리와 임시 디렉터리를 사용했다. 실제 업데이트를 설치하지 않았다.
- Chrome 장시간 실행, 실제 네트워크 공격·부하, EXE/Inno Setup 빌드, 배포 산출물, 설치 패키지 취약점 감사는 수행하지 않았다.
- 실제 사용자 채널 목록·설정·프로필 데이터는 읽거나 출력하지 않았다. 코드·설정 변경, 커밋·푸시·배포는 하지 않았다.
